# 收银渠道合规编排

本项目为支付收银台提供资金来源分类、展示方案、金融营销审核和页面回执追踪。银行卡与支付工具、消费信贷、资管产品、分期付款分别归类，可用于付款不代表可以混同展示或推广。

`fixtures/sample.json` 记录规则生效日、资金来源类别和禁止的营销表述。展示方案按地区与时间选择有效版本，仓库不保存真实账户、余额或交易凭据。

运行 `python3 service.py --check` 确认基础配置，执行 `python3 -m unittest -v` 检查服务身份与类别定义。启动服务后可访问 `/health`。
